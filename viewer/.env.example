# Copie para .env.local e preencha. A chave "anon"/"publishable" é feita pra ir no navegador
# (Project Settings > API no Supabase) — NUNCA coloque a service_role aqui.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# URL da API local (server/app.py). Só muda se o backend não estiver em localhost:5000.
VITE_API_URL=http://localhost:5000/api
